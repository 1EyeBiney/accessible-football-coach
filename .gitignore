# Real-world reference data. Development only, never shipped.
dev_data/

# Editor and OS noise
.vscode/
Thumbs.db
desktop.ini

# Per-machine Claude Code settings. The launch config below is shared.
.claude/settings.local.json
